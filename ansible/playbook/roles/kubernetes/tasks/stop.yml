# stop kubernetes agent.
- name: stop kubernetes agent
  command: systemctl stop kubelet.service
  tags:
    - stop

# stop docker.
- name: stop docker
  command: systemctl stop docker.service
  tags:
    - stop
