apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: deep-seek-coder
spec:
  selector:
    matchLabels:
      service: deep-seek-coder
  replicas: 1
  serviceName: deep-seek-coder
  template:
    metadata:
      labels:
        service: deep-seek-coder
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                - key: kubernetes.io/os
                  operator: In
                  values:
                    - linux
                - key: node-role.kubernetes.io/worker
                  operator: In
                  values:
                    - worker
                - key: type
                  operator: In
                  values:
                    - on-premise
                    - cloud
                - key: provider
                  operator: In
                  values:
                    - host
                    - aws
                    - azure
                - key: service
                  operator: In
                  values:
                    - virtualbox
                    - ec2
                    - eks
                    - aks
                - key: region
                  operator: In
                  values:
                    - local
                    - ap-south-1
                    - centralindia
                - key: availability_zone
                  operator: In
                  values:
                    - local-b
                    - ap-south-1b
                    - zone-2
      containers:
        - name: main
          image: sloopstash/ollama:v${AI_OLLAMA_VERSION}
          imagePullPolicy: IfNotPresent
          command: ["/usr/bin/supervisord"]
          args: ["-c", "/etc/supervisord.conf"]
          volumeMounts:
            - name: ollama-deep-seek-coder-model
              mountPath: /opt/ollama/model
            - name: ollama-deep-seek-coder-data
              mountPath: /opt/ollama/data
            - name: ollama-deep-seek-coder-log
              mountPath: /opt/ollama/log
            - name: supervisor-server
              mountPath: /etc/supervisord.conf
              subPath: supervisor-server
            - name: ollama-deep-seek-coder-supervisor
              mountPath: /opt/ollama/system/supervisor.ini
              subPath: supervisor.ini
          ports:
            - containerPort: 11434
          livenessProbe:
            tcpSocket:
              port: 11434
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 11434
            initialDelaySeconds: 15
            periodSeconds: 10
      volumes:
        - name: ollama-deep-seek-coder-model
          persistentVolumeClaim:
            claimName: ollama-deep-seek-coder-model
        - name: ollama-deep-seek-coder-data
          persistentVolumeClaim:
            claimName: ollama-deep-seek-coder-data
        - name: ollama-deep-seek-coder-log
          persistentVolumeClaim:
            claimName: ollama-deep-seek-coder-log
        - name: supervisor-server
          configMap:
            name: deep-seek-coder
        - name: ollama-deep-seek-coder-supervisor
          configMap:
            name: deep-seek-coder
            items:
              - key: supervisor.ini
                path: supervisor.ini
  volumeClaimTemplates:
    - metadata:
        name: ollama-deep-seek-coder-model
      spec:
        storageClassName: standard
        volumeMode: Filesystem
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
    - metadata:
        name: ollama-deep-seek-coder-data
      spec:
        storageClassName: standard
        volumeMode: Filesystem
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
    - metadata:
        name: ollama-deep-seek-coder-log
      spec:
        storageClassName: standard
        volumeMode: Filesystem
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 2Gi
