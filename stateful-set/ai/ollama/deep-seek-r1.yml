apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: deep-seek-r1
spec:
  selector:
    matchLabels:
      service: deep-seek-r1
  replicas: 1
  serviceName: deep-seek-r1
  template:
    metadata:
      labels:
        service: deep-seek-r1
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                - key: kubernetes.io/os
                  operator: In
                  values:
                    - linux
                - key: node-role.kubernetes.io/worker
                  operator: In
                  values:
                    - worker
                - key: type
                  operator: In
                  values:
                    - on-premise
                    - cloud
                - key: provider
                  operator: In
                  values:
                    - host
                    - aws
                    - azure
                - key: service
                  operator: In
                  values:
                    - virtualbox
                    - ec2
                    - eks
                    - aks
                - key: region
                  operator: In
                  values:
                    - local
                    - ap-south-1
                    - centralindia
                - key: availability_zone
                  operator: In
                  values:
                    - local-b
                    - ap-south-1b
                    - zone-2
      containers:
        - name: main
          image: sloopstash/ollama:v${AI_OLLAMA_VERSION}
          imagePullPolicy: IfNotPresent
          command: ["/usr/bin/supervisord"]
          args: ["-c", "/etc/supervisord.conf"]
          volumeMounts:
            - name: ollama-deep-seek-r1-model
              mountPath: /opt/ollama/model
            - name: ollama-deep-seek-r1-data
              mountPath: /opt/ollama/data
            - name: ollama-deep-seek-r1-log
              mountPath: /opt/ollama/log
            - name: supervisor-server
              mountPath: /etc/supervisord.conf
              subPath: supervisor-server
            - name: ollama-deep-seek-r1-supervisor
              mountPath: /opt/ollama/system/supervisor.ini
              subPath: supervisor.ini
          ports:
            - containerPort: 11434
          livenessProbe:
            tcpSocket:
              port: 11434
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 11434
            initialDelaySeconds: 15
            periodSeconds: 10
      volumes:
        - name: ollama-deep-seek-r1-model
          persistentVolumeClaim:
            claimName: ollama-deep-seek-r1-model
        - name: ollama-deep-seek-r1-data
          persistentVolumeClaim:
            claimName: ollama-deep-seek-r1-data
        - name: ollama-deep-seek-r1-log
          persistentVolumeClaim:
            claimName: ollama-deep-seek-r1-log
        - name: supervisor-server
          configMap:
            name: deep-seek-r1
        - name: ollama-deep-seek-r1-supervisor
          configMap:
            name: deep-seek-r1
            items:
              - key: supervisor.ini
                path: supervisor.ini
  volumeClaimTemplates:
    - metadata:
        name: ollama-deep-seek-r1-model
      spec:
        storageClassName: standard
        volumeMode: Filesystem
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
    - metadata:
        name: ollama-deep-seek-r1-data
      spec:
        storageClassName: standard
        volumeMode: Filesystem
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
    - metadata:
        name: ollama-deep-seek-r1-log
      spec:
        storageClassName: standard
        volumeMode: Filesystem
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 2Gi
