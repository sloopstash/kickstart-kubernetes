apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sloopstash-${ENVIRONMENT}-ctm-app-log
spec:
  storageClassName: sloopstash-ctm-app-log-local
  volumeName: sloopstash-${ENVIRONMENT}-ctm-app-log
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
